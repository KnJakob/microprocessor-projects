/*void setup() {
  Serial.begin(115200);
}

void loop() {
  int value = analogRead(A0);       // 0–1023, maps to 0–5V
  Serial.println(value);
  // no delay = fastest sampling; add delay(1) etc. to slow it down
}*/
